package com.guinetik.challenge.remote;

/**
 * Type interface for all remote api operations.
 * All feign interfaces must inherit from this interface in order to be compatible with the FeignApiFactory
 * @see FeignApiFactory
 */
public interface RemoteApi {
}
